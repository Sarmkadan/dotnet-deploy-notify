# ResultTestsJsonExtensions

Provides JSON serialization and deserialization extensions for `Result` and `Result<T>` types used in test scenarios. These methods facilitate converting test results to and from JSON strings for persistence, logging, or inter-process communication during automated testing workflows.

## API

### `ToJson`
